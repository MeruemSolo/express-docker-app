# Используйте официальный образ Node.js версии 20.15
FROM node:20.15

# Создать директорию для вашего приложения
WORKDIR /usr/src/app

# Установите зависимости до копирования основного кода
COPY package*.json ./
RUN npm install

# Копировать код самого приложения
COPY . .

# Приложение будет слушать 3000 порт
EXPOSE 3000

# Команда для запуска вашего приложения
CMD [ "node", "index.js" ]